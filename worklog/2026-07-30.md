# 2026-07-30 워크로그

## 개발팀 (대리 · 마이클 / 팀장 · 클레버)

- (마이클) `/api/address-coord` 엔드포인트 신설 — juso.go.kr 좌표제공서비스(addrCoordApi.do) 프록시
  - ENV: `JUSO_API_KEY2` (Cloudflare Pages 등록 완료)
  - addrCoordApi.do가 EPSG:5179 (UTMK TM 투영좌표) 반환함을 확인 → GRS80 TM 역투영으로 WGS84 변환
  - 테스트 성공: 강남파이낸스센터 → lon=127.0365086, lat=37.5000243 (강남역 동쪽 ~760m, 실측 일치 ✓)
- (마이클) `address-search.js` 필드 정리 — lon/lat 제거, admCd/rnMgtSn/udrtYn/buldMnnm/buldSlno 추가 반환 (coord 호출용)
- (클레버 지시·마이클 실행) 리팩터: projection 함수 → `functions/_lib/proj.js` 공용 모듈 분리, 디버그 필드(_tmX/_tmY) 제거
- 커밋: ee0cdd0 (refactor) / a011857 (feat) / 9ea45fa (fix TM 변환)

**다음 단계:** 프론트(취득세 계산기 등)에서 주소 선택 시 `/api/address-coord` lazy 호출 연결 (미구현)
